<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to "Makehub", root_path, :class => 'brand' %>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <%= nav_link 'Explore', projects_path %>
        </ul>
        <ul class="nav pull-right">
          <%= nav_link 'Add New Project', new_project_path, class: "hck-new-project-link" %>
          <% if signed_in? %>
            <%= nav_link 'My Labs', user_projects_path(current_user) %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <i class="icon-user icon-white"></i>
                <%= current_user.name %>
                <i class="caret"></i>
              </a>
              <ul class="dropdown-menu">
                <%= nav_link "My Account", user_path(current_user) %>
                <li class="divider"></li>
                <li><%= link_to "Log Out", signout_path, method: :delete %></li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign in with Google", "/auth/google_oauth2" %></li>
          <% end %>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>
